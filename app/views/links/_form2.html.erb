<%= form_with model: link2 do |form| %>
  <% if link2.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(link2.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
        <% link2.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :section %>
    <%= form.collection_select :section, Link.order(:section), :section, :section, include_blank: true %>
    <%= form.label :title %>
    <%= form.text_field :title %>
    <%= form.label :link %>
    <%= form.url_field :link %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>


  <div class="field_section">
    <%= form.label :section %>
    <%= form.text_field :section %>
  </div>

  <div class="actions_section">
    <%= form.submit %>
  </div>


<% end %>