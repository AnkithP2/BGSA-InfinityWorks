<p id="notice"><%= notice %></p>

<p>
  
  









<p id="notice"><%= notice %></p>

<br> 
<div class='card w-75 mx-auto text-center'>
  <div class='card-body' >
    <h1 class='card-title'>Event Information</h1>
 
    <h5 class='card-title' style='text-align:left;padding-left: 5%;'>
      <strong>Event ID:</strong>
      <%= @event.id %>
      <br>
      <strong>Title:</strong>
      <%= @event.title %>
      <br>
      <strong>Start Time:</strong>
      <%= @event.starttime.strftime('%B %d %Y, at %I:%M %p') %>
        <br>
      <strong>End Time:</strong>
      <%= @event.endtime.strftime('%B %d %Y, at %I:%M %p') %>
        <br>
      <strong>Event Points:</strong>
      <%= @event.eventpoints %>
        <br>
      <% if @admin %>
        <strong>Login Code</strong>
        <%= @event.logincode %>
      <br>
    

    <strong>RSVPd: <%= display_rsvpd %></strong>
    <br>
      <% for user in @users_rsvp do %>
        <%= User.find(user).firstname + " " + User.find(user).lastname + " "%> <br>
      <% end %>

    <strong>Attended:  <%= display_attended %> </strong>
    <br>
      <% for user in @users_attend do %>
        <p>
        <%= User.find(user).firstname + " " + User.find(user).lastname + " "%> <br>
        </p>
      <% end %>
  <%end%>
  <br>
  </h5>


    
    <style type="text/css">
        body { background: black; } 
    </style>
  </div>
</div>
<center><br>
<% if @admin %>
  <%= link_to 'Edit', edit_event_path(@event) %> |
  <%= link_to 'Delete', @event, method: :delete, data: { confirm: 'Are you sure?' } %> | 
<% end %>
  

<%= link_to 'Back', events_path %>
</center>