<%= form_with(model: admin_security) do |form| %>
  <% if admin_security.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(admin_security.errors.count, "error") %> prohibited this admin_security from being saved:</h2>

      <ul>
        <% admin_security.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :last_login %>
    <%= form.date_select :last_login %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
